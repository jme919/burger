<!DOCTYPE html>
<html lang="en">
<h1>Burgers</h1>


<div>
<h2>Burgers that have not been devoured</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
     <li>
       <p>
        {{burger_name}}
      </p>
     </li>

     {{/unless}}
     {{/each}}
</ul>

</div>

<div>
<form class="create-form">

  <div class="form-group">
    <input type="text" name="name" class="form-control" placeholder="Burger Name"></input>

  </div>

  <button type="submit" class = "btn btn-primary">Add Burger</button>
</form>
</div>



<div>
<h2>I devoured these!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      <li>
        <p>{{burger_name}}</p>
      </li>  
    {{/if}}
  {{/each}}
</ul>
</div>
